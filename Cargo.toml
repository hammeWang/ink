[workspace]
members = [
    "abi",
    "alloc",
    "core",
    "core/derive",
    "lang",
    "lang/macro",
    "prelude",
    "primitives",
]
exclude = [
    "examples/",
]

[profile.release]
panic = "abort"
lto = true
